#include <iostream>
#include "vector.h"
#include "shapes.h"

using namespace std;

int main() {
	/*
	Vector2 first_vector = Vector2(10.0, 30.0);
	Vector2 second_vector = Vector2(15.0, 5.0);
	Vector2 result = first_vector + second_vector;
	first_vector.add(second_vector);
	cout << "Result is: " << result;
	cout << "First vector is now: " << first_vector;
	result = first_vector + second_vector;
	cout << "Result is now: " << result;
	first_vector.add(second_vector);
	cout << "First vector is now: " << first_vector;


	cout << "=======================\n";
	Vector2 v3(1, 2);
	Vector2 v4(3, 4);
	Vector2 v5 = v3 + v4;
	cout << v3;
	cout << v4;
	cout << v5;
	cout << "Vector (1,2) times 2.5 = " << v3 * 2.5;

	Vector2 new_vector(5.0, 5.0);

	Circle c1(&new_vector, 5); //passing a pointer to new_vector, involves using a reference?
	cout << c1;

	c1.isInside(3, 2);
	c1.isInside(0, 5);
	c1.isInside(1, 1);
	c1.isInside(7, 6);

	cout << "==================\n\n"; */



	Circle::callPrintCircle();

	return 0;
}
